# JengaTrack — copy to .env and fill in real values (never commit .env)

# Supabase
SUPABASE_URL=https://yourproject.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Auth (dashboard JWT)
JWT_SECRET=your-32-character-random-string

# Twilio WhatsApp
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Exact public URL Twilio posts to (must match what is configured in the
# Twilio console). Required on Vercel because its catch-all rewrite injects a
# `?path=...` query param that breaks Twilio's HMAC signature check if we
# reconstruct the URL from req.url. Example:
#   https://build-monitor-lac.vercel.app/api/whatsapp-webhook
WEBHOOK_PUBLIC_URL=https://your-domain.vercel.app/api/whatsapp-webhook

# Set to 1 ONLY in local dev/tunnel setups where the public URL is awkward to
# pin down. NEVER set in production — it disables Twilio signature validation.
# SKIP_TWILIO_SIGNATURE=0

# OpenAI
OPENAI_API_KEY=sk-your-openai-key-here

# Google Gemini (fallback for intent, OCR, voice)
GEMINI_API_KEY=your_gemini_key_here

# App URL (used in WhatsApp messages and CORS)
DASHBOARD_URL=https://jengatrack.com

# Cron (daily heartbeat)
CRON_SECRET=your-random-cron-secret

# Environment
NODE_ENV=production
